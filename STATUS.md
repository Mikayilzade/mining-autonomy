# Status

Project state: **IN PROGRESS**

Last completed run: **Run 058 — residual validation + alternative-vocabulary spot-check**
Last updated: **2026-08-17**

## Completed research runs
Runs **001–058** are complete.

Latest durable files:
- `research/RUN_058_RESIDUAL_VALIDATION_AND_SPOTCHECK.md`
- `research/SOURCES_RUN_058.md`
- `research/CATALOG_ADDITIONS_RUN_058.md`

## Saturation state
Forty-one deliberate control/tail passes (Runs 018–058) have produced **0 new top-level economic mechanisms**. Taxonomy saturation confidence is **very high / effectively converged**.

Run 058 normalized every Run 057 residual candidate into already-known economic families. However, the required compact supplier-role spot-check still surfaced four additional independent provider projects: **Swan Inference, AntSeed, UsePod, and DeCloudX**. This is a non-negligible residual project cluster, so the project remains IN PROGRESS for one final narrow validation/control pass.

## Material Run 058 findings
Validated/normalized:
- **NodeOps Network Compute Provider** — live mainnet; VM/bare-metal supply supported; 2000 NODE + 200 NODE/CU bond; published base/performance rewards; strict network/root requirements.
- **OpenGPU Provider** — automatic GPU workload routing; no OGPU hardware lockup; still early-network demand/liquidity uncertainty.
- **Lium SN51** — GPU rental + Bittensor subnet emission; provider registration burns dynamic TAO fee; separate rental and emission revenue streams.
- **NodeAI Host** — curated qualified-datacenter GPU/VM hosting; host-set prices; 25% platform fee.
- **Kunagi Worker** — permissionless inference-worker concept is more concrete, but marketplace maturity, payout details and real utilization remain uncertain.
- **Scalattice Provider** — Windows/Linux inference provider agent; per-job earnings; $10 payout threshold; actual demand/fill remains key unknown.
- **SILO Storage Node** — PC/dedicated-server support; current published storage/egress payout formula; economics likely sensitive to fill and token liquidity.
- **Sentinel dVPN Node** — current Linux bandwidth-supplier node path reconfirmed.
- **UPGO.IO Relay Leaf** — Linux daemon/CLI and BNC reward claims; still WATCHLIST pending independent demand/liquidity evidence.
- **SOMA** — agent-compatible automated data submission is explicit, but checked quickstart remains testnet; liquid mainnet income not yet proven.

New Run 058 spot-check leads:
- **Swan Inference** — GPU providers serve inference; official page states USDC settlement.
- **AntSeed** — providers can sell inference, routing or AI-agent services and receive USDC for settled deliveries.
- **UsePod** — open inference marketplace for local GPU backends or upstream API resale, with USDC billing.
- **DeCloudX** — compute/GPU/storage/relay/validator operator tiers with staking and usage-linked earning claims.

## Mechanism conclusion
Run 058 found **0 new top-level mechanisms**. New projects map to existing families:
1. compute/GPU/inference marketplaces;
2. storage resource markets;
3. bandwidth/relay markets;
4. AI/data competition workers;
5. infrastructure/operator rewards;
6. agent/API/service marketplaces;
7. capital/stake-backed operator models.

## Current strongest candidates for later implementation research
For the specific `server bot finds simple paid work` objective:
1. PayanAgent request/bid worker;
2. OKX.AI A2A ASP automatic matching + active task intake;
3. agent2agent.market open machine-readable bounty/task feed;
4. AntSeed provider market — pending Run 059 validation;
5. UsePod provider/reseller market — pending Run 059 validation;
6. SOMA agent-compatible data submission / model competition path;
7. OKX.AI A2MCP paid services;
8. MCPize / a2a cloud where real buyer demand can be demonstrated;
9. automatic-routed inference suppliers such as Scalattice/OpenGPU/Swan if net economics are real.

## Economics / risk findings
- **Paid utilization/fill rate remains the dominant hidden variable** across compute, storage, bandwidth, agent and API markets.
- Token emissions/subsidies must be separated from customer-paid utilization.
- Bond/stake/collateral can make a technically simple node economically capital-heavy.
- New/small provider networks require production, token-liquidity and real-demand verification before promotion.
- USDC settlement is operationally simpler than illiquid reward tokens, but KYC/geography/off-ramp rules still require checking.
- A deployment guide or provider page does not by itself prove meaningful buyer demand.

## Current phase
The project now needs **Run 059 — validate the four Run 058 spot-check leads + repeat one compact control search.**

Completion confidence:
- taxonomy: **very high / effectively converged**
- paid-agent/x402/MCP/A2A mechanism saturation: **very high / converged**
- all-category mechanism saturation: **very high / converged**
- high-priority economics: **high**
- project-level saturation: **very high, pending one final residual cluster**
- overall completion: **not yet**

## Next run priority
**Run 059 — final residual-cluster validation + repeated compact supplier-role control.**

Validate only:
1. Swan Inference
2. AntSeed
3. UsePod
4. DeCloudX

For each capture:
- current production status;
- supplier admission mode;
- VPS / bare-metal / home-hardware eligibility;
- KYC and geography restrictions, including Azerbaijan if explicitly documented;
- payout asset and withdrawal path;
- stake/bond/collateral;
- recurring infrastructure/API cost;
- ToS / automation restrictions;
- real utilization/demand evidence if available;
- net-profit model inputs and key unknowns.

Then repeat one compact supplier-role search using alternate terms (`supplier`, `host`, `operator`, `worker`, `publisher`, `seller`, `partner`, `provider program`, `machine jobs`, `agent jobs`, `inference provider`, `storage operator`, `relay provider`).

### Completion logic
If Run 059 finds **0 new top-level mechanisms**, normalizes these four into existing families, and the repeated compact spot-check yields only duplicates/negligible new viable projects, set project state to **COMPLETE**, document remaining unknowns and final saturation evidence, and stop recurring research.

If Run 059 unexpectedly reveals another material cluster, keep IN PROGRESS and define only the smallest necessary follow-up.