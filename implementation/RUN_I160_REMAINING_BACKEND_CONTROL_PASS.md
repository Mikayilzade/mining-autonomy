# I160 — Remaining Resource / Execution Router backend control pass

Date: 2026-08-24
Status: **completed control checkpoint**

## Scope
Classified the remaining existing backend families without credentials, external API calls, account creation, CI dispatch, paid infrastructure, spend, task actions or value movement.

## Result
- `subscription_assistant` -> **SUPPORT_ONLY**. Existing ChatGPT/Codex subscription is fixed/sunk limited support capacity, not a free unlimited API and not a proven autonomous programmatic execution interface. Full monthly subscription price is not allocated to every task; finite capacity and opportunity/human-maintenance cost still matter.
- `cheap_external_api` -> **AUTHORIZATION_GATED_EVIDENCE_PREPARABLE**. First-party pricing/policy/rate-limit research and benchmark design can be prepared without credentials, but measured production materialization requires separately authorized credentials/live calls. Synthetic reference pricing cannot promote the route.
- `strong_external_api` -> **AUTHORIZATION_GATED_EVIDENCE_PREPARABLE** under the same fail-closed rule; stronger quality cannot substitute for real pricing, quota, reliability and acceptance measurements.
- `future_paid_vps` -> **SPEND_AND_INFRASTRUCTURE_AUTHORIZATION_GATED**. Provider/spec/pricing planning can be prepared, but rental and measured production capacity require separate spend/infrastructure authorization. Monthly rental is fixed committed cost requiring a task-volume allocation basis; it is not charged wholly to every task and is not treated as economically free after rental.

## Boundary
No remaining external backend can autonomously become a current `measured_reproducible` production route from the present environment. This is a control boundary, not project completion: `python_local`/`owned_pc` still need genuine local measurements, external APIs need authorization for live materialization, and VPS needs spend authorization.

The deterministic-first routing rule remains unchanged: cheap deterministic filtering/Python -> selective AI only when required -> cheapest backend meeting acceptance probability and positive conservative expected margin. Watcher architecture remains cheap polling/webhook/WebSocket where ToS permits -> local dedupe/filter -> selective AI, without product/rate-limit bypass.

## Safety
No network production observation, credentials, account creation, API execution, CI dispatch, infrastructure rental, spend, paid-task acceptance, settlement or value movement occurred.

## Next action
Advance the existing I138 experiment-readiness/control chain with I160 classifications. Preserve the unresolved PayanAgent geography/provider-access gate and exact bounded-observation authorization gate. Do not reopen discovery.
