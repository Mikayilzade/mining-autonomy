# Handoff for Any Future Chat / Agent

Do not reconstruct this project from chat memory. Read repository state first.

## Resume protocol
Read `START_HERE.md`, `STATUS.md`, `METHODOLOGY.md`, `HANDOFF.md`, `RUN_LOG.md`, `CATALOG.md`, `implementation/RUN_LOG.md`, and latest files named in `STATUS.md`.

## Current checkpoint
Discovery Runs **001–062 COMPLETE**. Implementation Runs through **I166 COMPLETE as scoped checkpoints**. Project: **IMPLEMENTATION IN PROGRESS**.

Latest broad stages:
- I156: exact I154-bound local runtime chain executed; I113 = **PASS_BLOCKED**, 7/7 clean.
- I157: GitHub-hosted `free_tier_ci` = **SUPPORT_TESTING_ONLY** for generic external paid work.
- I158: `local_model` no-spend branch exhausted in this environment; no usable model/GPU interface observed.
- I159: portable fail-closed `owned_pc` evidence packet; physical-PC measurements still required.
- I160: remaining backend control pass completed. `subscription_assistant` is support-only; external APIs require separately authorized credentials/live measurement; future VPS requires separate spend/infrastructure authorization.
- I161: fail-closed experiment boundary; default state **FAIL_CLOSED_EXTERNAL_BOUNDARIES** and user-PC measurement selected as next inert branch.
- I162: portable user-PC measurement procedure; ownership confirmation + provenance-bound benchmark/availability/energy/tariff/opportunity cost required. Focused tests: **4 passed**.
- I163: deterministic benchmark/session wrapper for quality, latency, reliability and actually exercised safe parallelism.
- I164: lightweight benchmark core removed the heavy calibration import graph while preserving benchmark identity/digest. Exact I159/I162/I163/I164 focused closure: **6 passed**.
- I165: one-shot local user-PC materializer; benchmark fields come only from I163; external benchmark overrides rejected. Combined exact-current I163/I164/I165 focused tests: **9 passed**.
- I166: real-evidence gate in front of I165. Requires ownership confirmation, complete external evidence groups, valid ranges/counter order, and rejects fixture/example/synthetic/placeholder/dummy/mock provenance. Focused tests authored; not yet claimed as exact-source executed.

## Current control chain
`I113 exact runtime PASS_BLOCKED -> Resource/Execution Router evidence ladder/control pass -> I161/I162/I163/I164/I165/I166 user-PC materialization boundary -> I050/I066/I123 -> I130/I131/I133 conservative economics -> I136 portfolio -> I137 fallback -> I138 readiness -> I142/I145/I148 source evidence -> I143 selection -> I140 bounded observation -> I141 economic-test packet`.

## Runtime/resource result
Runtime-regression evidence is materially demonstrated for the exact I156 snapshot and authorizes nothing by itself.

`python_local` lacks trustworthy measured energy + explicit tariff provenance here. `free_tier_ci` is support/testing-only. `local_model` has no usable interface in this environment. `owned_pc` now has repository-side I159/I162/I163/I164/I165/I166 semantics, but no real user-PC evidence session has actually been produced.

I163 may use Python-visible logical CPU count only to bound candidate concurrency. It accepts safe parallelism only when that level is actually exercised and all work units succeed with the exact expected output. It does not infer availability, energy, tariff or opportunity cost.

I165 imports benchmark measurements only from I163 and cannot accept caller overrides for them. I166 adds a production-intent gate for external evidence: null templates remain non-evidence and fixture/example/synthetic/placeholder provenance is rejected.

I166 cannot prove a provenance string is truthful. Benchmark quality/latency/reliability/parallelism, availability, before/after joule readings, electricity tariff and opportunity cost must still be genuinely measured/provided with provenance. Synthetic energy/tariff, CPU-count-as-parallelism, one-run reliability, inferred 24/7 availability and zero opportunity cost from sunk ownership/subscription are forbidden substitutions.

## Source result
PayanAgent remains the active machine-task source, but public-doc geography is converged at `WAIT_FOR_POLICY_CONTACT_OR_SEPARATELY_AUTHORIZED_LOCAL_ACCESS`. Do not repeat geography searches unless new first-party material appears. Endpoint reachability is not provider-country eligibility evidence.

## Immediate next broad run
First execute the I166 focused tests in an exact source-bound local materialization when available, without dispatching CI solely for this result.

Then run I166/I165 on the actual user-owned PC with explicit ownership confirmation and only genuine provenance-bound external facts. If no trustworthy joule counter exists, keep energy blocked rather than estimating it.

If I162 reaches `USER_PC_PACKET_COMPLETE`, pass that result into I050/I066/I123 -> I130/I131/I133 -> I136/I138 and evaluate conservative economics. Evidence assembly does not authorize production market observation, credentials, task acceptance, spend or value movement.

## Hard/resource boundary
No spend, KYC, wallets, paid work acceptance, publication, settlement, real credentials, registration, CAPTCHA/geofence/rate-limit/product-limit bypass or value movement without separate explicit authorization. No production PayanAgent task-list/receipt GET has occurred.

Permitted future sub-hour watchers use cheap polling/webhook/WebSocket -> local dedupe/filter -> selective AI only within provider/API/ToS limits. Fixed/sunk and marginal cost, energy, quota/opportunity cost, API/model fees, retries, maintenance, watcher overhead and payment risk remain separate.

## Git/CI
Keep automatic runtime triggers disabled. GitHub Actions remains support/testing-only for this project; do not dispatch it as a monetization compute backend.

## Completion
Implementation remains incomplete until a permitted real test demonstrates positive economics or reasonable candidates are exhausted and confirmed by control passes.
