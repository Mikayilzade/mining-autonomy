# Handoff for Any Future Chat / Agent

Do not reconstruct this project from chat memory. Read repository state first.

## Resume protocol
Read `START_HERE.md`, `STATUS.md`, `METHODOLOGY.md`, `HANDOFF.md`, `RUN_LOG.md`, `CATALOG.md`, `implementation/RUN_LOG.md`, and latest files named in `STATUS.md`.

## Current checkpoint
Discovery Runs **001–062 COMPLETE**. Implementation Runs **I001–I141 COMPLETE as scoped checkpoints**. Project: **IMPLEMENTATION IN PROGRESS**.

Latest broad stage: `implementation/RUN_I139_I141_BOUNDED_ECONOMIC_TEST_DESIGN.md`.

## Current control chain
`I113 exact runtime -> I128/I129 local resource measurement -> I050/I066/I123 -> I130/I131/I133 conservative economics -> I136 portfolio -> I137 fallback -> I138 readiness -> I140 bounded read-only observation design -> I141 economic-test packet`.

I139 hardened portfolio orchestration against duplicate backend/evidence definitions, unknown watcher-budget IDs and one-shot iterable loss. I140 defines a bounded no-spend public observation manifest with explicit policy/rate-limit/geography/CAPTCHA checks, request caps and stop rules. I141 combines readiness, conservative route and the observation design into the first concrete economic-test manifest and specifies the demand/payout/fee/latency fields that a real read-only window must measure.

## Immediate next broad run
Do not create another stack of micro safety wrappers. At the first executable exact-current checkout, run the complete local chain in one broad cycle. Materialize `python_local` only from current runtime evidence plus genuine energy telemetry and explicit tariff provenance. Then rerun I136/I138.

If local survives, instantiate I140/I141 for the highest-ranked currently permitted public source only after current policy evidence is obtained, then request/use exactly the bounded read-only authorization represented by the manifest. If local fails, move through I137/I134 to the next existing no-new-spend branch (currently free/conditional CI under the model) and apply the same framework.

A positive read-only observation can justify designing a later tiny real-task test, but never implies permission to accept/submit paid work, use credentials, spend, or settle value.

## Hard boundary
No spend, KYC, wallets, paid work acceptance, publication, settlement, real credentials, CAPTCHA/geofence/rate-limit/product-limit bypass or value movement without separate explicit authorization. No production market observation has occurred yet.

## Resource boundary
- ChatGPT/Codex subscription is fixed/sunk limited support, not a free autonomous API.
- Watchers may use permitted sub-hour polling via ordinary code/webhook/WebSocket/cron, but deterministic filter/dedupe precedes selective AI and provider limits are obeyed.
- Fixed/sunk cost, true marginal cost, energy, quota/opportunity cost, AI/API fees, retries, maintenance, watcher overhead, observation overhead and platform/payment risk remain explicit.
- Free/conditional CI is not assumed unlimited/free; paid APIs and future VPS preserve credentials/spend/infrastructure gates.

## Git/CI
Keep automatic runtime triggers disabled. Prefer broad coherent stages. Expected fail-closed outcomes belong in artifacts rather than notification-generating failures.

## Completion
Implementation remains incomplete until a permitted real test demonstrates positive economics or reasonable candidates are exhausted and confirmed by control passes.