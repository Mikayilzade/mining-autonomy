# Handoff for Any Future Chat / Agent

Do not reconstruct this project from chat memory. Read repository state first.

## Resume protocol
Read `START_HERE.md`, `STATUS.md`, `METHODOLOGY.md`, `HANDOFF.md`, `RUN_LOG.md`, `CATALOG.md`, `implementation/RUN_LOG.md`, and latest files named in `STATUS.md`.

## Current checkpoint
Discovery Runs **001–062 COMPLETE**. Implementation Runs **I001–I153 COMPLETE as scoped checkpoints**. Project: **IMPLEMENTATION IN PROGRESS**.

Latest broad stage: `implementation/RUN_I151_I153_SOURCE_BOUND_RUNTIME_TRANSPORT.md`.

## Current control chain
`source-bound exact-current snapshot -> I113 exact runtime -> I128/I129 local resource measurement -> I050/I066/I123 -> I130/I131/I133 conservative economics -> I136 portfolio -> I137 fallback -> I138 readiness -> I142/I145/I148 PayanAgent source evidence -> I143 source selection -> I140 bounded observation -> I141 economic-test packet`.

## Runtime result
Direct `git clone` remains unavailable in the current execution container because `github.com` DNS resolution fails. I151 removes that transport-specific dependency without relaxing source integrity: a trusted connector/export may materialize files only when each file matches its Git blob SHA and the bundle is bound to explicit repository/commit/tree identity. Missing, duplicate, invalid or tampered snapshots fail closed.

Focused I151 verification executed locally: **2 passed**. This is not an I113 PASS. The next executable step is to materialize the complete current I106–I113 dependency/artifact closure through the source-bound path and then execute I113. I106 still computes/validates the dependency closure and banned-network imports.

## Source result
PayanAgent remains the active machine-task source, but the public-doc geography branch is converged at `WAIT_FOR_POLICY_CONTACT_OR_SEPARATELY_AUTHORIZED_LOCAL_ACCESS`. Do not repeat geography searches unless new first-party material appears. Endpoint reachability alone is not provider-country eligibility.

## Immediate next broad run
Prioritize runtime/resource execution. Build the complete current source-bound runtime bundle and run I113. If it passes, continue in the same broad cycle through I128/I129 -> I050/I066/I123 -> I133/I136 -> I138 wherever genuine evidence exists. Never guess energy or tariff.

If `python_local` cannot be materialized or fails conservative economics, use I137/I134 to move immediately to the next existing no-new-spend backend branch. Do not reopen discovery.

## Hard boundary
No spend, KYC, wallets, paid work acceptance, publication, settlement, real credentials, registration, CAPTCHA/geofence/rate-limit/product-limit bypass or value movement without separate explicit authorization. No production PayanAgent task-list/receipt GET has occurred.

## Resource boundary
ChatGPT/Codex subscription is fixed/sunk limited support, not a free autonomous API. Permitted sub-hour watchers use cheap polling -> local dedupe/filter -> selective AI only within provider limits. Fixed/sunk and marginal cost, energy, quota/opportunity cost, API/model fees, retries, maintenance, watcher/observation overhead and payment risk remain separate. Free/conditional CI is not assumed unlimited/free; paid APIs and future VPS preserve authorization gates.

## Git/CI
Keep automatic runtime triggers disabled. Exact-current source identity can use the source-bound snapshot path; do not weaken source binding merely because direct git transport is unavailable.

## Completion
Implementation remains incomplete until a permitted real test demonstrates positive economics or reasonable candidates are exhausted and confirmed by control passes.